@model Proyecto_Api.DTO.PostDTO

@{
    ViewData["Title"] = "Detalle Post";
}

<h2>@ViewData["Title"]</h2>

<div class="post-details">
    <div class="form-group">
        <label>ID del Usuario:</label>
        <span>@Model.UserId</span>
    </div>
    <div class="form-group">
        <label>Título:</label>
        <span>@Model.Title</span>
    </div>
    <div class="form-group">
        <label>Cuerpo:</label>
        <p>@Model.Body</p>
    </div>
</div>

<a href="@Url.Action("ListaPosts", "Home")" class="btn btn-secondary">Volver al inicio</a>